// we want to write a function that takes two arguments and returns their minimum.
const minimize = (num1, num2) => {
  return console.log(Math.min(num1, num2));
}

// test case should return 7
minimize(7,9);
